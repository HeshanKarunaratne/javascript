const first = [1, 2, 3];
const second = [4, 5, 6];

//Combine 2 Arrays
const combined = first.concat(second);

//Slice an Array 
const slice = combined.slice(2);
console.log(combined);
console.log(slice);